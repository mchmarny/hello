name: push

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  
jobs:

  test:
    uses: ./.github/workflows/test.yaml
    secrets:
      token: ${{ secrets.SNYK_TOKEN }}
      go_version: ${{ secrets.GO_VERSION }}